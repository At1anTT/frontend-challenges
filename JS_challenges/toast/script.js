const horisontalPosition = document.getElementById(
  'horisontal-position-selector'
);
const verticalPosition = document.getElementById('vertical-position-selector');

const typeWindow = document.getElementById('type-selector');

const messageForWindow = document.getElementById('toast-message-input');

const durrationValue = document.getElementById('range-input');

